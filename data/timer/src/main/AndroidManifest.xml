<?xml version="1.0" encoding="utf-8"?>
<!--
  Rest-timer foreground service (Fusion design doc Phase 3 steps 2-3).
  POST_NOTIFICATIONS is requested at runtime by the UI; without it the
  service keeps running and cues still fire (Xiaomi/MIUI fallback).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>
        <service
            android:name="com.dropsync.data.timer.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Rest timer countdown for workout sets" />
        </service>
    </application>
</manifest>
