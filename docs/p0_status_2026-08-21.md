# P0-Statusverifikation (Stand 2026-08-21)

Quelle: `docs/Kritische Befunde.md`. Jeder Befund wurde gegen den aktuellen
Code geprueft. Ergebnis: 12 von 13 Befunden sind bereits behoben. Der
Befund-Dokument ist veraltet und sollte als historisch markiert werden.

| # | Befund | Status | Evidenz |
|---|--------|--------|---------|
| 1 | Kalibrierung vs. Live-Signalvertrag | BEHOBEN | `CalibrationController.kt:490-494`: Sweep laeuft ausschliesslich auf GP ("Umbauplan Phase 1.3"). Profil speichert `signalKind` (`SensorModels.kt:95`, Default SIGNED_GYRO_PROJECTION) |
| 2 | Gespeicherte Schwellen != kalibrierte Schwelle | BEHOBEN | `CalibrationViewModel.kt:115-128`: theta wird direkt persistiert (`detectionThreshold = result.theta`), keine SPK/NPK-Rekonstruktion mehr |
| 3 | Halbe Reps zaehlen als vollstaendig | BEHOBEN | `PhaseValidator.kt:49-59`: Einphasen-Kurzschluss entfernt, beide Phasen zwingend; RepPipelineTest fordert exakt 2 |
| 4 | Disconnect/Wechsel brechen Set nicht ab | BEHOBEN | `TrainViewModel.kt:575-580`: zentrale idempotente `abortActiveSet(reason)`; `ActiveSetController` (domain/sensor) haelt Lifecycle |
| 5 | Kalibrierung nach spaeterem Verbinden nicht geladen | BEHOBEN | `TrainViewModel.kt:583-610`: combine(selectedExercise, connectedDeviceId).distinctUntilChanged().flatMapLatest |
| 6 | BLE-Verbindung kann dauerhaft haengen | BEHOBEN | `BleSensorProvider.kt:274-281`: GattEvent.OperationTimedOut -> cleanupConnection(GATT_TIMEOUT); MtuNegotiator mit Timeout/Retry/Fallback (Testinfra-Plan 5a) |
| 7 | Remote-Disconnect raeumt nicht auf | BEHOBEN | `BleSensorProvider.kt:477-498`: zentrale cleanupConnection(reason) beendet Polling-Jobs, MTU-Timeout, JitterBuffer, DedupTracker, GATT |
| 8 | Paketverlust verfaelscht Zeitbasis | BEHOBEN | `PeakDetector.kt:38-39,173-175`: refractoryMs/expectedDurationMs in Millisekunden, inRefractory prueft Timestamps; RepCounter.abortPending() bei Luecken (Phase 2.6) |
| 9 | Lernpfad leert Daten zu frueh | BEHOBEN | `TrainViewModel.kt:271-289`: unveraenderlicher SetTrace via finishAndTakeTrace(), learnFromTrace(trace, reps) arbeitet auf der Kopie |
| 10 | Refiner lernt aus zu wenig Peaks | BEHOBEN | `CalibrationRefiner.kt:45-53`: peaks.size != correctedReps -> null; zweiphasige Peaks Pflicht; Revalidierung durch echte Pipeline (Zeile 87-89); Step-Limits (Phase 7.3); Kandidaten-Revisionen + Rollback (ProfileLearningPolicy) |
| 11 | Playback-Transition spielt zwei Titel | BEHOBEN | TransitionManager existiert nicht mehr (ADR-konform refactored); PlaybackRepositoryImpl nutzt Custom-Kommando an den einen sessionfuehrenden Player |
| 12 | Custom Commands melden Erfolg zu frueh | BEHOBEN | `PlaybackRepositoryImpl.kt:147-151,174-189`: awaitResult() wartet das ListenableFuture ab, throwOnFailure() wirft bei Fehlercodes |
| 13 | Analysefehler dauerhaft gecacht | BEHOBEN | `TrackAnalysisRepositoryImpl.kt:188-220`: permanente vs. temporaere Fehler getrennt; temporaer -> Result.retry() |

## Zusatzpruefungen

- **Lint**: `lintDebug --continue` ueber alle 17 Module: BUILD SUCCESSFUL,
  keine Baseline, keine abortOnError-Abschaltung, keine lint.xml. Die im
  Befund-Dokument gelisteten 6 Modulfehler sind behoben.
- **PlaybackService-Sicherheit**: `isOwnPackage()` (PlaybackService.kt:278-279)
  beschraenkt interne Kommandos aufs eigene Package.
- **Timer-Snapshot**: Skip/Finish loeschen synchron (runBlocking clear(),
  TimerService.kt:108,126).
- **SensorHealth-UI**: TrainScreen zeigt Signalqualitaet an (DEGRADED-Hinweis,
  TrainScreen.kt:660,694).

## Verbleibende offene Punkte

1. **AudioTrackTimestampReader nicht DI-verdrahtet**: implementiert, aber die
   echte Latenzmessung pro Route (RouteProfileStore) nutzt noch statische
   Profile. Folgearbeit.
2. **Gate 11b**: accel/orientation-Flags bleiben false bis Hardware-Freigabe
   (bewusst, ADR-0014).
3. **Ground-Truth-Traces**: echte Hardware-Datensaetze fehlen weiterhin
   (Paket E des Umbauplans, braucht M5Stick).
