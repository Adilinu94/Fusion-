# Handoff — Session 2026-08-12

**Session-ID:** Claude-814d9738 (Fortsetzung) + diese Session
**Commit:** `04dfe6f` (HEAD master, origin/master)
**Zustand:** Clean working tree, alles committed und gepusht.

## Was wurde erreicht

1. **MtuNegotiator (5a)** in `data/sensor/` — pure Entscheidungslogik (512/23/Retry 2/GATT_ERROR 133), 5 Unit-Tests, kompiliert grün. Integration in `BleSensorProvider.kt` (aktuell MTU 185 hartkodiert) ist noch offen.

2. **Punkt 3 komplett** (Timer-Snapshot/Recovery + MtuNegotiator + core:testing-Fake-Umbau), Commit `01e638d`.

3. **Shadow-Konflikt gelöst (Rebase):** Remote-Shadow (`feature/workout/shadow/`) behalten, eigene domain/data-Shadow-Implementierung gelöscht. `TrainViewModelTest.kt` auf `:core:testing`-Fakes portiert.

4. **Rep-Zählungs-Analyse + Recherche:** Pipeline-Parameter dokumentiert, 6 Schwachstellen (A1–A6), 5 Architekturmängel (B1–B5), 9 priorisierte Verbesserungen. Forschungsliteratur recherchiert (Balestra, RecoFit, LiftRight, Yurtman, Filippou, Prabhu, Lim).

5. **Umbauplan geschrieben:** `docs/design/REP_ZAEHLUNG_UMBAUPLAN_2026-08-12.md` — 9 Punkte mit Code-Diffs, Dateipfaden, Zeilenreferenzen, Testanweisungen, Abnahmekriterien. Neu in diesem Commit (`04dfe6f`).

## Nächste Schritte (Priorität)

1. **Punkte 1–3 (SOFORT) umsetzen:** Shadow-Engine-Achsenfix, SPK/NPK-Laden, Qualitäts-Schwellen-Vereinheitlichung. ~3h Arbeit, vollständig im Umbauplan spezifiziert.

2. **MtuNegotiator-Integration:** `BleSensorProvider.kt` Z. 230 (`gattClient.requestMtu(REQUIRED_MTU)`) ersetzen durch `MtuNegotiator.onMtuChanged()`-Loop mit Timeout/Retry/Fallback.

3. **RestTimerRecovery-Hilt-Verdrahtung:** `TimerDataModule.kt` — `recover()` bei App-Start, `TimerService` binden.

4. **Punkte 4–6 (MITTELFRISTIG):** Accel-Kanal, Multi-Template, adaptive Refraktär.

## Blockierer

- **Gradle-Executor-Bug:** Windows/JDK 25/Gradle 9.5 — `testDebugUnitTest` auf Android-Modulen startet nicht. Nur JVM-Module testbar.
- **Graphify-Index:** Stale (Index `1b206aae` vs HEAD `04dfe6f`). Vor nächster Graphify-Nutzung neu indexieren.

## Wichtige Dateien

| Datei | Relevanz |
|---|---|
| `docs/design/REP_ZAEHLUNG_UMBAUPLAN_2026-08-12.md` | **NEU** — Hauptplan für die nächste Arbeit |
| `docs/design/SHADOW_DIFF_HARNESS_PLAN.md` | Shadow-Diff-Harness (Recorder, JSONL, 5 Gates) |
| `docs/design/TESTINFRASTRUKTUR_UMBAUPLAN_2026-08-10.md` | Testpyramide, 5a/5b |
| `docs/STATUS_FORTSCHRITT.md` | Letzter Stand: Abschnitt F (c7f2a9e1); Umbauplan fehlt noch |
| `domain/sensor/.../ExerciseEnginePipeline.kt` | Kern der Rep-Zählung |
| `feature/workout/.../TrainViewModel.kt` | Einbettung (Live + Shadow) |
| `data/sensor/.../MtuNegotiator.kt` | Neu, Integration offen |
| `data/sensor/.../BleSensorProvider.kt` | MTU 185 hartkodiert, MtuNegotiator einbauen |

## Git-Hinweise

- Vor jedem Push: `git fetch && git rebase origin/master` (letztes Mal kollidiert wegen Parallelarbeit)
- Remote hatte eigene Shadow-Commits (`9216db2`, `6d30a76`): **bei Konflikt remote-Version behalten**
- `feature/train`-ADR nicht umgesetzt (verfrüht laut letzter Entscheidung)